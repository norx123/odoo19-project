<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- ============================================================
         LOAN REQUEST VIEWS
         ============================================================ -->

    <!-- Form View -->
    <record id="view_loan_request_form" model="ir.ui.view">
        <field name="name">custom.loan.request.form</field>
        <field name="model">custom.loan.request</field>
        <field name="arch" type="xml">
            <form string="Request for Loan">
                <header>
                    <button name="action_submit" string="Submit"
                            type="object" class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_approve" string="Approve"
                            type="object" class="btn-primary"
                            invisible="state != 'submitted'"
                            groups="hr.group_hr_manager,hr.group_hr_user"/>
                    <button name="action_refuse" string="Refuse"
                            type="object"
                            invisible="state != 'submitted'"
                            groups="hr.group_hr_manager,hr.group_hr_user"/>
                    <button name="action_cancel" string="Cancel"
                            type="object"
                            invisible="state not in ('draft', 'submitted')"/>
                    <button name="action_reset_draft" string="Reset to Draft"
                            type="object"
                            invisible="state not in ('refused', 'cancel')"
                            groups="hr.group_hr_manager"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,submitted,approved"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Employee Information">
                            <field name="employee_id"
                                   readonly="state in ('approved', 'refused', 'cancel')"/>
                            <field name="department_id" readonly="1"/>
                            <field name="company_id"
                                   readonly="state in ('approved', 'refused', 'cancel')"
                                   groups="base.group_multi_company"/>
                        </group>
                        <group string="Loan Details">
                            <field name="request_date"
                                   readonly="state in ('approved', 'refused', 'cancel')"/>
                            <field name="loan_type"
                                   readonly="state in ('approved', 'refused', 'cancel')"/>
                            <field name="loan_amount"
                                   readonly="state in ('approved', 'refused', 'cancel')"/>
                            <field name="currency_id"
                                   readonly="state in ('approved', 'refused', 'cancel')"
                                   groups="base.group_multi_currency"/>
                        </group>
                    </group>
                    <group>
                        <group string="Repayment">
                            <field name="installment_count"
                                   readonly="state in ('approved', 'refused', 'cancel')"/>
                            <field name="installment_amount" readonly="1"/>
                            <field name="repayment_start_date"
                                   readonly="state in ('approved', 'refused', 'cancel')"/>
                        </group>
                    </group>
                    <group string="Reason / Purpose">
                        <field name="reason" nolabel="1"
                               placeholder="Describe the purpose of the loan..."
                               readonly="state in ('approved', 'refused', 'cancel')"/>
                    </group>
                    <notebook>
                        <page string="Approver(s)" name="approvers">
                            <field name="approver_ids"
                                   readonly="state in ('approved', 'refused', 'cancel')">
                                <list editable="bottom">
                                    <field name="approver_id" string="Approver"/>
                                    <field name="required" string="Required"/>
                                    <field name="status" string="Status" readonly="1"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- List View -->
    <record id="view_loan_request_list" model="ir.ui.view">
        <field name="name">custom.loan.request.list</field>
        <field name="model">custom.loan.request</field>
        <field name="arch" type="xml">
            <list string="Loan Requests"
                  decoration-success="state == 'approved'"
                  decoration-warning="state == 'submitted'"
                  decoration-danger="state == 'refused'">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="department_id"/>
                <field name="request_date"/>
                <field name="loan_type"/>
                <field name="loan_amount"/>
                <field name="installment_count"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'approved'"
                       decoration-warning="state == 'submitted'"
                       decoration-danger="state == 'refused'"
                       decoration-info="state == 'draft'"/>
            </list>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_loan_request_search" model="ir.ui.view">
        <field name="name">custom.loan.request.search</field>
        <field name="model">custom.loan.request</field>
        <field name="arch" type="xml">
            <search string="Search Loan Requests">
                <field name="name" string="Reference"/>
                <field name="employee_id"/>
                <field name="loan_type"/>
                <filter string="My Requests" name="my_requests"
                        domain="[('employee_id.user_id', '=', uid)]"/>
                <separator/>
                <filter string="Draft" name="draft"
                        domain="[('state','=','draft')]"/>
                <filter string="Submitted" name="submitted"
                        domain="[('state','=','submitted')]"/>
                <filter string="Approved" name="approved"
                        domain="[('state','=','approved')]"/>
                <filter string="Refused" name="refused"
                        domain="[('state','=','refused')]"/>
                <separator/>
                <filter string="Employee" name="group_employee" domain="[]"
                        context="{'group_by': 'employee_id'}"/>
                <filter string="Loan Type" name="group_loan_type" domain="[]"
                        context="{'group_by': 'loan_type'}"/>
                <filter string="Request Date" name="group_date" domain="[]"
                        context="{'group_by': 'request_date'}"/>
            </search>
        </field>
    </record>
</odoo>
